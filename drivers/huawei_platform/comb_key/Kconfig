#
# comb key info
#

menuconfig HW_COMB_KEY
    bool "Comb key info"
    default n
    help
      Say yes if you want device detect driver.
      This is for device connected version. If unsure, say N here.